// found very difficult unable to complete

 #include<string.h>
#include<stdio.h>
#include<stdlib.h>

char splitoverspace(char inpstring[400]);
char largest_word(char inpwords[200,20]);

void main()
{
    char inpstring[400], wordlist[200,20];
    int i,j,k=0;
    printf("Text (paragraph, no newline character):\n");
    gets(inpstring);

    //splitting into words
    for (i=0;inpstring[i]!='\0';i=i+j)    //each character in inpstring
    {
        word="";
        for (j=0;inpstring[i+j]!=' ';j++) //clubbing characters into a word
        {
            if
            strcat(word,inpstring[j]);
        }
        word[j]='\0';
        wordlist[k]=word;
    }
    for (i=0;i<)


}
